# ACHSE.respo – Axiom-4 Version

ACHSE = /achse/

ROOT.compat  = TMP
ACHSE.compat = TMP
TMP.compat   = TMP
LAGE.compat  = TMP
laTER.compat = TMP
ORT.compat   = TMP

FREQ = 12e

D1 = ACHSE

SEQ.360 = D1 → D2 → D3 → D4 → D5 → D0 → D1

ACTIVE = TRUE
